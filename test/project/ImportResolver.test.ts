import { ImportResolver } from '../../src/project/ImportResolver';

describe('Project ImportResolver', () => {
    // If different from modules/ImportResolver
  it('should be defined', () => {
      expect(ImportResolver).toBeDefined();
  });
});
