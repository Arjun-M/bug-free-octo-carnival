import { ProjectBuilder } from '../../src/project/ProjectBuilder';

describe('ProjectBuilder', () => {
    // Assuming builder pattern
  it('should be defined', () => {
      expect(ProjectBuilder).toBeDefined();
  });
});
